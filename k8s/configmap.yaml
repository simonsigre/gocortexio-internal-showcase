apiVersion: v1
kind: ConfigMap
metadata:
  name: cortex-showcase-config
  namespace: cortex-showcase
  labels:
    app: cortex-showcase
data:
  api-url: "https://showcase.cortex.example.com"
  base-url: "/"
  # Add other configuration as needed
  github-api-enabled: "true"
  max-projects-per-page: "50"
